package com.decagon.assignments;

public class Main {
    public static void main(String[] args) {

    }

    public int shiftedDiff(String first, String second){

        return -1;
    }

}

/*
*const shiftedDiff = (first, second) => {
  if(first.length !== second.length) return -1;
  if(first === second) return 0;
  let len = first.length - 1;
let temp;
let counter = 0;

for (let j = 0; j < len+1; j++) {
    let i = 0;
    temp = first[len]
    counter++
    while (i < len) {
        temp += first[i];
        i++;
    }
//     console.log(temp);
    if(temp === second) return counter;
    first = temp;
}
  return -1;
};
 */
